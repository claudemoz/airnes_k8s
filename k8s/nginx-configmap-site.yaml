apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config-site
data:
  nginx.conf: |
    server {
      listen 80;
      server_name airnes.tsiang.fr;

      location / {
          root /usr/share/nginx/html;
          index index.html;
          try_files $uri $uri/ /index.html;
      }
    }
